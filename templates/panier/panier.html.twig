{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!
{% endblock %}

{% block body %}
    
{# {{dump(panier)}} #}
	{# {% if panier is not empty%}
		<table border="1" class="table table-striped table-inverse">
			<thead>
				<tr>
					<th>Nom du produit</th>
					<th>Quantité</th>
				</tr>
			</thead>
			<tbody>
				{% for contenupanier in panier %}
					<tr>
						<td>{{panier.contenupanier.produit}}</td>
						<td>{{panier.contenupanier.quantite}}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	{% else %} #}
		<p>Le panier est à un utilisateur</p>
	{# {% endif %} #}
{% endblock %}
